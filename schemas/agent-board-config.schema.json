{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Agent Board Project Config",
  "description": "Per-project configuration for Agent Board. Place this file at .agent-board/config.json in the workspace root. All VS Code settings can be overridden here.",
  "type": "object",
  "properties": {
    "github": {
      "type": "object",
      "description": "GitHub integration settings for this project.",
      "properties": {
        "owner": {
          "type": "string",
          "description": "GitHub repository owner (user or organization)."
        },
        "repo": {
          "type": "string",
          "description": "GitHub repository name."
        }
      },
      "additionalProperties": false
    },
    "jsonProvider": {
      "type": "object",
      "description": "JSON task provider settings.",
      "properties": {
        "path": {
          "type": "string",
          "description": "Workspace-relative or absolute path to the JSON tasks file. Default: .agent-board/tasks"
        }
      },
      "additionalProperties": false
    },
    "beadsProvider": {
      "type": "object",
      "description": "Beads CLI provider settings.",
      "properties": {
        "executable": {
          "type": "string",
          "description": "Path to the Beads CLI executable."
        }
      },
      "additionalProperties": false
    },
    "worktree": {
      "type": "object",
      "description": "Git worktree settings. When enabled, providers that support worktree will create an isolated worktree for each task.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to create a git worktree for providers that support it. Default: true."
        }
      },
      "additionalProperties": false
    },
    "genAiProviders": {
      "type": "object",
      "description": "Per-provider GenAI configuration. Global providers (chat, cloud, copilot-cli) can be overridden here. Project providers (ollama, mistral) are enabled and configured only here.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Whether this provider is enabled."
          },
          "model": {
            "type": "string",
            "description": "Model name to use with this provider."
          },
          "endpoint": {
            "type": "string",
            "description": "API endpoint URL for this provider."
          }
        },
        "additionalProperties": false
      }
    },
    "kanban": {
      "type": "object",
      "description": "Kanban board settings.",
      "properties": {
        "columns": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Ordered list of Kanban column identifiers."
        }
      },
      "additionalProperties": false
    },
    "squad": {
      "type": "object",
      "description": "Agent squad settings.",
      "properties": {
        "maxSessions": {
          "type": "number",
          "minimum": 1,
          "maximum": 50,
          "default": 10,
          "description": "Maximum number of parallel agent sessions launched by the squad feature."
        },
        "sourceColumn": {
          "type": "string",
          "default": "todo",
          "description": "Column from which the squad picks tasks to launch. Default: todo."
        },
        "activeColumn": {
          "type": "string",
          "default": "inprogress",
          "description": "Column tasks are moved to when the agent starts working. Default: inprogress."
        },
        "doneColumn": {
          "type": "string",
          "default": "review",
          "description": "Column tasks are moved to when the agent completes. Default: review."
        }
      },
      "additionalProperties": false
    },
    "notifications": {
      "type": "object",
      "description": "VS Code notification settings for automatic task state changes.",
      "properties": {
        "taskStarted": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when a task is automatically moved to the active column."
        },
        "taskCompleted": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when a task is automatically moved to the done column."
        }
      },
      "additionalProperties": false
    },
    "pollInterval": {
      "type": "number",
      "minimum": 5000,
      "description": "Polling interval in milliseconds for providers that use polling."
    },
    "logLevel": {
      "type": "string",
      "enum": ["DEBUG", "INFO", "WARN", "ERROR"],
      "description": "Log level for the Agent Board output channel."
    }
  },
  "additionalProperties": false
}
